# Concepts

Background on how `py-bootstrap` is put together. Useful when writing your own
bootstraps or extending the tool.

- {doc}`architecture` — bootstraps, operations, files processors, and discovery.

```{toctree}
:hidden:

architecture
```
