# CLI reference

Generated from the `bootstrap` argument parser. Bootstrap-specific sub-commands
under `build` come from each installed bootstrap, so this page reflects whatever
is available in the environment the docs were built in.

```{eval-rst}
.. argparse::
   :module: py_bootstrap.scripts.bootstrap
   :func: build_parser
   :prog: bootstrap
```
