# Reference

- {doc}`cli` — every `bootstrap` command and option, generated from the parser.
- {doc}`api` — the Python API, generated from docstrings (for extending the tool).

```{toctree}
:hidden:

cli
api
```
